<Window x:Class="MarkPredictor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarkPredictor"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="Grade Prediction Record Tool" FontSize="22" FontWeight="Bold"/>
        </Grid>
        <Grid Grid.Row="1" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Exit" Name="exitButton" HorizontalAlignment="Left" Margin="2,2,2,2"  Height="25" Width="100" Click="exitButton_Click" />
            <Button Grid.Column="1" Content="Go" HorizontalAlignment="Left" Margin="2,2,2,2" Height="25" Width="100" Click="button_Click"/>
        </Grid>
        
    </Grid>
</Window>
